# Comma-separated bearer tokens — add one per uptime-monitor instance that connects
AGENT_TOKENS=your-secret-token-1,your-secret-token-2

# Region label shown in /health and stored in heartbeat records
AGENT_REGION=cambodia

# Port (default: 3001)
PORT=3001
